name: Run xcrun

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-13, macos-14]
      fail-fast: false

    steps:
    - run: xcrun --show-sdk-version
